package net.tomasherman.replayvault.client.logic

object ConfigKeys {
  val username = "credentials.username"
  val password = "credentials.password"

  val service = "service.url"
  val rescanRate = "rescan_rate"
  val directories = "managed.directories"
}
